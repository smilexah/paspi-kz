export type StepT = {
    number: number;
    text: string;
};

export type StepperSliderT = {
    id: number;
    badge: string;
    title: string;
    subtitle: string;
    imageSrc: string;
    imageAlt: string;
}